---
interface Props {
    link: string;
    title: string;
}
const { link, title } = Astro.props;
---

<li>
    <a href=`${link}`>{title}</a>
</li>

<style>
    li {
        @apply text-base font-semibold;
        a {
            @apply px-6;
        }
    }
</style>
